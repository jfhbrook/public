# Test building the main branch with tito
test:
  spec="$(pwd)/yq.spec" outdir="$(pwd)" make -f .copr/Makefile

# Apply copr configuration
apply:
  coprctl apply -f ./copr.yml

publish:
  git push origin
  copr build-package jfhbrook/joshiverse --name yq
